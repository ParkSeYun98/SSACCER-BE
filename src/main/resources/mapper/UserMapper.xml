<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.ssaccer.model.dao.UserDao">

    <insert id="insertUser" parameterType="User">
        INSERT INTO users (userId, password, name, nickname, role)
        VALUES (#{userID}, #{password}, #{name}, #{nickname}, #{role})
    </insert>

    <select id="selectUserByUserId" parameterType="String" resultType="User">

    </select>

    <select id="selectUserByUserSeq" parameterType="Integer" resultType="User">

    </select>

    <select id="selectUserList" resultType="User">

    </select>

    <update id="modifyUser" parameterType="User">

    </update>

    <delete id="removeUserByUserId" parameterType="String">

    </delete>

    <delete id="removeUserByUserSeq" parameterType="Integer">

    </delete>

</mapper>